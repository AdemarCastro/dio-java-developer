package com;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");

        /*Em linguagens de programação como Java, palavras reservadas (também conhecidas como "palavras-chave") são termos especiais com significados específicos na sintaxe da linguagem. Essas palavras têm funções pré-definidas e não podem ser usadas para nomear variáveis, classes ou métodos, pois são reservadas para fins específicos do compilador ou da máquina virtual Java.

        As palavras reservadas começam com a letra minuscula

        Nomalmente as IDEs nos ajudam a identificar essas palavras com cores diferentes

        Algumas das principais palavras reservadas em Java incluem:

        public: Modificador de acesso que indica que uma classe, método ou atributo pode ser acessado por qualquer parte do programa.

        private: Modificador de acesso que indica que uma classe, método ou atributo só pode ser acessado dentro da própria classe.

        protected: Modificador de acesso que indica que uma classe, método ou atributo pode ser acessado dentro da própria classe e em suas subclasses.

        static: Indica que uma variável ou método pertence à classe em vez de pertencer a instâncias individuais da classe. Variáveis estáticas são compartilhadas por todas as instâncias da classe.

        final: Usado para indicar que uma variável não pode ser alterada (constante) ou que uma classe não pode ser estendida (final class) ou que um método não pode ser sobrescrito (final method).

        void: Usado para indicar que um método não retorna nenhum valor.

        class: Usado para definir uma classe.

        if: Usado para iniciar uma estrutura condicional (se).

        else: Usado em conjunto com o if para definir um bloco de código a ser executado se a condição do if não for atendida (senão).

        while: Usado para definir um loop enquanto uma condição é verdadeira.

        for: Usado para definir um loop com uma inicialização, uma condição de continuação e um incremento.

        Essas são apenas algumas das palavras reservadas em Java. É importante lembrar que essas palavras têm significados específicos na linguagem e não podem ser usadas como identificadores (nomes de variáveis, classes, métodos) no código Java.*/

    }
}